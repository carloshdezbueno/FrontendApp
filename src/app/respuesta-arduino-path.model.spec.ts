import { RespuestaArduinoPath } from './respuesta-arduino-path.model';

describe('RespuestaArduinoPath', () => {
  it('should create an instance', () => {
    expect(new RespuestaArduinoPath()).toBeTruthy();
  });
});
